function CarpentryView({ token }) {
    return (
        <div>
            <h2>Gestión de Carpintería (LAN-WOD1)</h2>
            <p>Este es un marcador de posición para el módulo de gestión de carpintería.</p>
        </div>
    );
}
