function DataProtectionView({ token }) {
    return (
        <div>
            <h2>Gestión de Protección de Datos (LAN-DPR2)</h2>
            <p>Este es un marcador de posición para el módulo de gestión de protección de datos.</p>
        </div>
    );
}
