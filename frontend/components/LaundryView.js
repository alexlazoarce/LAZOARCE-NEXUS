function LaundryView({ token }) {
    return (
        <div>
            <h2>Gestión de Lavandería (LAN-LDR3)</h2>
            <p>Este es un marcador de posición para el módulo de gestión de lavandería.</p>
        </div>
    );
}
