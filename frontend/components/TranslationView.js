function TranslationView({ token }) {
    return (
        <div>
            <h2>Gestión de Traducción (LAN-TRN5)</h2>
            <p>Este es un marcador de posición para el módulo de gestión de traducción.</p>
        </div>
    );
}
